<%= form_for(@party_set) do |f| %>
  <% if @party_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@party_set.errors.count, "error") %> prohibited this party_set from being saved:</h2>

      <ul>
      <% @party_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :notes %><br />
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
